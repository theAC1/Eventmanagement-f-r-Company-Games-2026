// game-punkte.ts — server-only scoring logic.
// This module runs in the api-server (artifacts/api-server/src/lib/game-punkte.ts).
// It is stubbed here to prevent import errors in the Vite frontend build.
// Do NOT import this from any client component — use /api/ergebnisse endpoints instead.
export {};
